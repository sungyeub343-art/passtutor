# Repository workflow

- This repository contains the Edupass tutoring website deployed with GitHub Pages.
- After every requested website change, validate the affected files and browser behavior.
- Commit completed changes to `main` with a concise commit message.
- Push `main` to `origin` so the GitHub Pages workflow publishes the update.
- Do not commit credentials, tokens, or private customer data.